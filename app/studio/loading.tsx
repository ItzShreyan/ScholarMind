import { RouteLoading } from "@/components/dashboard/RouteLoading";

export default function StudioLoading() {
  return (
    <RouteLoading
      title="Opening studio"
      copy="Loading the active study stack so you can jump straight back into revision."
    />
  );
}
